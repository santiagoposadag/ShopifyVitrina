{
  "name": "vitrina",
  "version": "0.1.0",
  "private": true,
  "description": "Vitrina — a WhatsApp-native inventory assistant for Shopify.",
  "workspaces": [
    "server"
  ],
  "scripts": {
    "dev:server": "npm run dev -w server",
    "test": "npm run test -w server",
    "build": "npm run build -w server",
    "docs:check": "bash scripts/check-docs.sh"
  },
  "engines": {
    "node": ">=20"
  }
}
